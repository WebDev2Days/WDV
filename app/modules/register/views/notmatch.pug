extends index.pug

block message
  block append scripts
    script.
      $(window).on('load',function(){
        $('#passNotMatch').modal('show');
      });
  #passNotMatch.modal.fade(tabindex='-1' role='dialog' aria-labelledby='passNotMatchTitle' aria-hidden='true')
    .modal-dialog(role='document')
      .modal-content
        .modal-header
          h5#passNotMatchTitle.modal-title Submission Failed
          button.close(type='button' data-dismiss='modal' aria-label='Close')
            span(aria-hidden='true') ×
        .modal-body
          p Password did not match.
        .modal-footer
          button.btn.btn-secondary(type='button', data-dismiss='modal') Close
